minish-1.0$ export x="     <tab>     "
minish-1.0$ $x
minishell:              : command not found

--------------------------------------------------

echo "                                         ''              "

l's              '

--------------------------------------------------

minish-1.0$ export x="'"
minish-1.0$ echo $x yassine $x
yassine
minish-1.0$ echo $x yassine $x
yassine

--------------------------------------------------

minish-1.0$ ls >> $X
syntax error near unexpected token `newline'
minish-1.0$ echo $?
258

--------------------------------------------------

declare -x x="a b"
declare -x a
declare -x b="yassine"
declare -x y="a"
minish-1.0$ export y=$x

--------------------------------------------------

inish-1.0$ cd ..
cd: error retrieving current directory: getcwd: cannot access parent directories: No such file or directory
minish-1.0$ echo $?
0
minish-1.0$
-------------------------------------------------

// if (flag == 1)
{
	// if (!whitespace_before_equal(expand))
	// 	token_add_back(tokens, new_token(remove_quotes(expand), 0));
	// else
	// 	add_to_list_token(expand, false, tokens);
}
// else if (ft_strchr(word, '$') && flag == 1)
// 	add_to_list_token(expand, true, tokens);
// else if (ft_whitespace(expand) && flag == 3)
// 	token_add_back(tokens, new_token(NULL, 0));
// else if (ft_isempty(expand))
	// 	token_add_back(tokens, new_token(NULL, 0))

--------------------------------------------------
export a="text  " //TODO
